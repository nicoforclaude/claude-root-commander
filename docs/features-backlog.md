# Features Backlog

## Permission Handling

- Add `--dangerously-skip-permissions` flag to Claude modes
  - Enable by default for specific projects (configurable in settings)

## Custom Modes

- Per-project custom modes (saved in settings)